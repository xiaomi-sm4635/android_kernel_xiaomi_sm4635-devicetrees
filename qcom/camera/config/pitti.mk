dtbo-$(CONFIG_ARCH_PITTI) := pitti-camera.dtbo
dtbo-$(CONFIG_ARCH_PITTI) += pitti-camera-sensor-mtp.dtbo
dtbo-$(CONFIG_ARCH_PITTI) += pitti-camera-sensor-qrd.dtbo
dtbo-$(CONFIG_ARCH_PITTI) += warm-camera-sensor-qrd.dtbo
